{
  "name": "@coretexa/mcp",
  "version": "0.1.0",
  "description": "Reference MCP server for the Coretexa accountability queue — work an agent can't close itself.",
  "type": "module",
  "bin": { "coretexa-mcp": "src/index.js" },
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node test/smoke.js"
  },
  "keywords": ["mcp", "ai-agents", "accountability", "ticketing", "verification"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "pg-mem": "^3.0.0"
  },
  "engines": { "node": ">=18" }
}
