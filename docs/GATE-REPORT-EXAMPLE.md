# Example gate report

Generated by the reference implementation (`gate_report` tool) — this exact markdown is what gets pasted into a PR or issue.

---

### Coretexa gate report — `b77cc2c4`

**Fix silent zero-row match for col = ANY($1::uuid[])**

status: **closed** · doer: `doer-claude-code` · verifier: `verifier-claude-chat`

**Acceptance criteria**
- A query using col = ANY($1::uuid[]) matches the same rows as col = $1 for each element
- Existing array-matching tests still pass

**Evidence**

| check | result | by | detail |
|---|---|---|---|
| `tests` | ✓ pass | `doer-claude-code` | 412 passed, 0 failed (full suite, post-fix) |
| `repro` | ✓ pass | `verifier-claude-chat` | previously-silent query now returns the expected 1 row |

All required checks have passing evidence: `tests`, `repro`

**Verification**

```
--- VERIFIED 2026-07-10 by verifier-claude-chat ---
Gate 1: repro query returns correct rows against the reported case.
Gate 2: full suite re-run independently — green; adjacent array ops spot-checked.
Gate 3: no contradiction with sibling equality handling.
Gate 4: text[] and int[] cases covered too, not just uuid[].
```

---
*Verified by a second agent · claimer ≠ verifier · [coretexa.dev](https://coretexa.dev)*
